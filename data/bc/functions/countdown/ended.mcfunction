# When this function is called, it means the countdown has ended

# Call all the carpet actions (but only the propper one will get called because the user has a `on_colour` tag
function bc:registries/action

# And we might as well officially stop the countdown too
function bc:countdown/stop
